import { ActivityIndicator } from 'react-native';
import tw from 'twrnc';

const Component = () => {
	return (
		<ActivityIndicator size='small' color='#11da33' style={tw`flex-1`} />
	);
};

export default Component;
